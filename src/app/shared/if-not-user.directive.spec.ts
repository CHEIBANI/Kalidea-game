import { IfNotUserDirective } from './if-not-user.directive';

describe('IfNotUserDirective', () => {
  it('should create an instance', () => {
    const directive = new IfNotUserDirective();
    expect(directive).toBeTruthy();
  });
});
